name: Test

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:


jobs:
  setup:
    uses: sunxyw/dumb/.github/workflows/setup-environment.yml@main
    with:
      environment: mysql
      php-version: 8.0
      operating-system: ubuntu-latest
